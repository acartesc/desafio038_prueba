<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <p class="nav-link disabled"> <%= link_to "ToDo's", root_path %> </p>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item">
          <p class="nav-link disabled"> Tareas completadas: <%= current_user.lists.where(done: true).count %> / <%= current_user.lists.count %> </p>
        </li>
        <li class="nav-item">
          <p class="nav-link disabled"><%= link_to "Salir", destroy_user_session_path, method: :delete %></p>
        </li>
      <% end %>
    </ul>

  </div>
</nav>
